<div class="checkbox">
    <label class="_checkbox_label">
		<p class="field switch">
			<label class="cb-enable"><span>{{ativo}}</span></label>
			<label class="cb-disable"><span>{{inativo}}</span></label>
			<input type="checkbox" name="{{name}}" id="{{id}}" class="checkboxStatusList" {{moreContent}} {{checked}}>
		</p>
    </label>
</div>